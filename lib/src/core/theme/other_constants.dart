/// Constants for customizing animation behavior.
const double sigmaX = 50; // Horizontal blur radius for the animation.
const double sigmaY = 50; // Vertical blur radius for the animation.
const Duration duration =
    Duration(milliseconds: 150); // Duration for the animations.

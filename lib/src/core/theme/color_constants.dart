import 'package:flutter/material.dart';

class DarkColorConstants {
  static const bgColor = Colors.black;
  static const inputBgColor = Color.fromARGB(102, 0, 0, 0);
  static const mainTextColor = Colors.white;
  static const secondTextColor = Color.fromARGB(153, 255, 255, 255);
  static const appBarIconsColor = Colors.white;
  static const onBgColor = Color.fromARGB(77, 18, 18, 18);
  static const primaryColor = Color(0xff0735FF);

  static const inActiveIconsColor =
      Color.from(alpha: 0.60, red: 1, green: 1, blue: 1);
}

class LightColorConstants {
  static const bgColor = Colors.white;
  static const inputBgColor = Color.fromARGB(102, 240, 240, 240);
  static const mainTextColor = Colors.black;
  static const secondTextColor = Color.fromARGB(153, 0, 0, 0);
  static const appBarIconsColor = Colors.black;
  static const onBgColor = Color.fromARGB(77, 240, 240, 240);
  static const primaryColor = Color(0xff0735FF);

  static const inActiveIconsColor = Color.fromARGB(153, 0, 0, 0);
}
